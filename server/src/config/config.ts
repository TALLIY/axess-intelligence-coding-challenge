const config = {
    mysql: {
        host: 'localhost',
        user: 'root',
        password: 'dilahk',
        database: 'axess_data_dump'
    },
    server: {
        host: 'localhost',
        port: 5000
    }
}

export default config;